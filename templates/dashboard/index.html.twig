{% extends 'base.html.twig' %}

{% block title %}Hello {{ user.name ?: user.email }}{% endblock %}

{% block body %}

{#{% for item in recent %}#}
    {#<div>{{ dump(item) }}</div>#}
{#{% endfor %}#}

<section>
    <h2>Recent log</h2>
    {% if recent %}
        {{ include('budget/_budget.html.twig', {recent: recent}) }}
        <a class="btn btn-primary btn-block" href="{{ path('budget') }}">More items</a>
    {% else %}
        <div class="mb-3">You have no records yet.</div> <div><a href="{{ path('budget_add') }}" class="btn btn-success">Create a record</a></div>
    {% endif %}
</section>

{% endblock %}
